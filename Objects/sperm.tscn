[gd_scene format=3 uid="uid://nkck8iu8e4ld"]

[ext_resource type="Texture2D" uid="uid://bo0httbtc75f1" path="res://Objects/sperminator-Sheet.png" id="1_4royl"]
[ext_resource type="Script" uid="uid://c0j8la7khm48j" path="res://Objects/sperm.gd" id="2_d3cng"]

[sub_resource type="Shader" id="Shader_d3cng"]
code = "// NOTE: Shader automatically converted from Godot Engine 4.6.stable's CanvasItemMaterial.

shader_type canvas_item;
render_mode blend_mix;
uniform int particles_anim_h_frames;
uniform int particles_anim_v_frames;
uniform bool particles_anim_loop;

uniform float pixel_size : hint_range(2.0, 16.0) = 2.0;
uniform bool handle_edges = false;

varying vec2 p_world;

void vertex() {
	p_world = (MODEL_MATRIX * vec4(VERTEX, 0.0, 1.0)).xy;
	
	float h_frames = float(particles_anim_h_frames);
	float v_frames = float(particles_anim_v_frames);
	VERTEX.xy /= vec2(h_frames, v_frames);
	float particle_total_frames = float(particles_anim_h_frames * particles_anim_v_frames);
	float particle_frame = floor(INSTANCE_CUSTOM.z * float(particle_total_frames));
	if (!particles_anim_loop) {
		particle_frame = clamp(particle_frame, 0.0, particle_total_frames - 1.0);
	} else {
		particle_frame = mod(particle_frame, particle_total_frames);
	}	UV /= vec2(h_frames, v_frames);
	UV += vec2(mod(particle_frame, h_frames) / h_frames, floor((particle_frame + 0.5) / h_frames) / v_frames);
}

void fragment() {
	vec2 p_snapped = floor(p_world / pixel_size) * pixel_size + pixel_size / 2.0;
	vec2 delta = p_snapped - p_world;

	mat2 M_p = mat2(dFdx(p_world), dFdy(p_world));
	mat2 M_uv = mat2(dFdx(UV), dFdy(UV));
	mat2 J = M_uv * inverse(M_p);
	vec2 delta_uv = J * delta;

	vec2 uv = UV + delta_uv;
	vec4 color = texture(TEXTURE, uv);

	if (handle_edges) {
		float margin_x = (abs(J[0].x) + abs(J[1].x)) * pixel_size * 0.5;
		float margin_y = (abs(J[0].y) + abs(J[1].y)) * pixel_size * 0.5;
		if (uv.x < margin_x || uv.x > 1.0 - margin_x || uv.y < margin_y || uv.y > 1.0 - margin_y) {
			color.a = 0.0;
		} else {
			color.a = step(0.5, color.a);
		}
	} else {
		color.a = step(0.5, color.a);
	}

	COLOR = color;
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_j74um"]
shader = SubResource("Shader_d3cng")
shader_parameter/particles_anim_h_frames = 7
shader_parameter/particles_anim_v_frames = 1
shader_parameter/particles_anim_loop = false
shader_parameter/pixel_size = 2.0
shader_parameter/handle_edges = false

[node name="sperm" type="CPUParticles2D" unique_id=95014920]
material = SubResource("ShaderMaterial_j74um")
scale = Vector2(-1, 1)
amount = 1
texture = ExtResource("1_4royl")
lifetime = 0.5
preprocess = 0.1
spread = 0.0
gravity = Vector2(0, 0)
initial_velocity_min = 400.0
initial_velocity_max = 400.0
radial_accel_min = -600.0
radial_accel_max = -600.0
damping_min = 100.0
damping_max = 100.0
scale_amount_min = 0.8
scale_amount_max = 1.4
anim_speed_min = 1.0
anim_speed_max = 1.0
script = ExtResource("2_d3cng")

[node name="Timer" type="Timer" parent="." unique_id=1046861064]
wait_time = 0.35
one_shot = true
autostart = true

[connection signal="timeout" from="Timer" to="." method="_on_timer_timeout"]
